# Copy this file to .env.local and fill in the values.
# DATABASE_URL is handled automatically by docker-compose — no action needed.

# Get a free API key at https://www.setlist.fm/settings/api
SETLISTFM_API_KEY=your_setlist_fm_api_key_here
